# Copy this file to .env and fill in your Supabase project's values
# (Project Settings → API in the Supabase dashboard).
#
# The anon key is safe to ship inside the desktop app — access to every
# table it can reach is enforced by Row Level Security policies in the
# Supabase project, not by keeping this key secret. NEVER put the
# service_role key here or anywhere in the app — that key bypasses RLS.

SUPABASE_URL=https://wnuhfyjmesurylehejwy.supabase.co
SUPABASE_ANON_KEY=sb_publishable_y7ZIrF5o5U1qc7d1ni1nHA_zqAMeHsv
